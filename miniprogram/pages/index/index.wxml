<view class="container">
  <view class="banner-warn" wx:if="{{banner}}">{{banner}}</view>

  <view class="scan-entry" bindtap="onScan">
    <view class="scan-emoji">📷</view>
    <view class="scan-text">
      <view class="scan-title">扫一扫</view>
      <view class="muted scan-sub">支持小程序码 / 链接类二维码</view>
    </view>
  </view>

  <scroll-view scroll-x class="board-chips">
    <view class="chips chips-line">
      <view
        class="chip {{board === item.key ? 'active' : ''}}"
        wx:for="{{boards}}"
        wx:for-item="item"
        wx:key="key"
        bindtap="onBoardTap"
        data-key="{{item.key}}"
      >{{item.label}}</view>
    </view>
  </scroll-view>

  <view class="chips" wx:if="{{board === 'price'}}">
    <view
      class="chip {{priceSort === item.key ? 'active' : ''}}"
      wx:for="{{sorts}}"
      wx:for-item="item"
      wx:key="key"
      bindtap="onSortTap"
      data-key="{{item.key}}"
    >{{item.label}}</view>
  </view>

  <view class="card">
    <view class="muted lb-note">{{note}}</view>
    <view class="lb-row" wx:for="{{rows}}" wx:key="id">
      <view class="lb-rank {{item.isFree ? 'lb-free' : ''}}">{{item.rank}}</view>
      <view class="lb-main">
        <view class="lb-name-line">
          <text class="lb-name">{{item.name}}</text>
          <text class="tag">{{item.tagText}}</text>
        </view>
        <view class="muted lb-sub">{{item.provider}}<text wx:if="{{item.cnyText}}">  {{item.cnyText}}</text></view>
      </view>
      <view class="lb-metric price-main">{{item.metric}}</view>
    </view>
  </view>

  <view class="footer-note">
    价格整理自公开网络，仅供参考，以各官方页面为准；本工具不构成购买建议。
  </view>
</view>
